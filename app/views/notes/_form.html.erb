<%= form_with(model: [ @patient, note ], local: true) do |form| %>

  <% if @patient.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@patient.errors.count, "error") %> prohibited
        this patient from being saved:
      </h2>
      <ul>
        <% @patient.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :note %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.label :doctor %><br>
    <%= form.text_field :doctor %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
